import AccessControlledModel from 'girder/models/AccessControlledModel';

var ContainerModel = AccessControlledModel.extend({
    resourceName: 'dm/testing/container'
});

export default ContainerModel;
